# Instructions

All project instructions live in [`CLAUDE.md`](../CLAUDE.md) at the repo root — architecture,
commands, design principles, and Python/JavaScript guidelines. Follow that file; this one exists
only as a pointer so there is a single source of truth.
