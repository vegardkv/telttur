# Telttur configuration
fylke: Akershus

# Buffer distance around roads in meters
buffer_distance_m: 1000

# Output filename
output_filename: map_akershus.html

# Tentability scoring: composite lake suitability score
scoring:
  enabled: true
